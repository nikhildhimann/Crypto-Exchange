
  # Crypto Wallet

  Crypto Wallet is a multi-chain wallet frontend built with Vite and React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
